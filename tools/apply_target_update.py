"""검증한 변경을 한 번 적용한다. 기준/결과 해시가 다르면 중단한다."""
from pathlib import Path
from hashlib import sha256
ROOT = Path(__file__).resolve().parents[1]

EDITS = [('src/attach_receipts.py',
  '2dd0466679b68a4fecbb852673e040fe5fa8594cb53df6c341dfd1b0e2bc7471',
  '087f39dc55458fc1373c579be6a5cfb2142393e4c4e217f0fdef465342f07cbd',
  [(10, 12, '매칭 규칙: 금액과 허용 범위 내 거래일로 찾고, 가맹점 구별과 1:1 관계까지\n확인한다. 불완전하거나 구별되지 않는 관련 후보가 있으면 해당 거래만 보류한다. 카드 매입 처리 때문에\n'),
   (32, 32, 'from .target_matching import VENDOR_MIN, VENDOR_MARGIN\n'),
   (33, 34, 'from .concur_rows import rows_complete, rows_observed, readiness_detail, snapshot_signature\n'),
   (271, 274, 'def rows_when_ready(page, tries: int = 90, wait_ms: int = 500,\n                    allow_incomplete: bool = False) -> list[Row]:\n    """목록이 안정될 때까지 기다린다. 대상별 처리에서는 불완전 행도 보존한다."""\n    previous, repeats = None, 0\n'),
   (277, 279, '        observed = rows_observed(rows) if allow_incomplete else complete\n        signature = snapshot_signature(rows)\n        repeats = repeats + 1 if observed and signature == previous else 0\n        # 늦게 채워지는 필드와 영구적인 미확인을 혼동하지 않도록 불완전 행은 더 기다린다.\n        if observed and repeats >= (3 if not complete else 1):\n'),
   (280, 281, '        previous = signature if observed else None\n'),
   (288, 293, ''),
   (294, 330, '    """대상별 매칭. 불완전 후보 보존, 중복/애매한 거래 보류, 순서 배정 금지."""\n    from .target_matching import match as safe_match\n    return safe_match(slips, rows, tolerance_days)\n'),
   (392, 393, '    rows = rows_when_ready(page, allow_incomplete=True)\n'),
   (407, 408, '        print(f"  알림: {len(rows) - len(dated)}건은 값을 읽지 못해 관련 거래만 보류합니다")\n'),
   (413, 414, '    pairs, skipped = match(slips, rows, tolerance)\n'),
   (491, 492, '    return int(bool(skipped))\n')]),
 ('src/concur_driver.py',
  '24d5634dd32b72175d539da01eaab200d084bcc802029d678999c7c33663f545',
  '825e1115a344cb1864503c6b2155581673169449437bc455f4986df756a6561c',
  [(11, 11, '        self.matching = None\n'),
   (19, 20, '        return ar.rows_when_ready(self.page, allow_incomplete=True)\n\n    def check_binding(self, intended, rows):\n        if self.matching is not None and not self.matching.verify(intended, rows):\n            raise ar.AttachError(\'보류: 현재 목록에서 대상 경비를 유일하게 확인하지 못했습니다.\')\n'),
   (22, 23, '        current = self.rows()\n        self.check_binding(row, current)\n        matches = [r for r in current if r.expense_id == row.expense_id]\n        found = matches[0] if len(matches) == 1 else None\n'),
   (48, 48, '        if self.matching is not None:\n            self.check_binding(plan.row, self.rows())\n')]),
 ('src/concur_rows.py',
  'eb9b6e314fbb57e50853b78ec5f64a4e1d794e7600ab9bdd9de474bd5c6a48be',
  'e39afb35c5a68d59dcac4e48457e2ebe2d88a885227768eba04d67d4b81fb8a1',
  [(83, 83, '\n\ndef rows_observed(rows):\n    """목록 로딩/누락은 차단하되 안정된 불완전 행은 매칭 단계에 그대로 넘긴다."""\n    return bool(rows) and not any(getattr(r, \'read_problem\', \'\') for r in rows)\n\n\ndef snapshot_signature(rows):\n    """해석값뿐 아니라 원문 변화도 재렌더링/대상 변경으로 감지한다."""\n    return tuple((r.expense_id, r.when, r.amount, r.vendor, r.expense_type,\n                  r.has_receipt, r.receipt_file, r.raw_date, r.raw_amount,\n                  getattr(r, \'read_problem\', \'\')) for r in rows)\n')]),
 ('src/concur_workflow.py',
  'aa604efc1a7f7426fb8cee1e0329ca353004ea1909f675409d69ce6f437c8590',
  'e249612d9b81f9a35fdeab03ddb126fa89ac4327a71e728a5d76678fe65bebf2',
  [(4, 5, 'from . import attach_receipts as ar, fix_expenses as fx, console, sheet\nfrom .target_matching import match_sources\n'),
   (17, 17, '    matching: object\n'),
   (42, 43, '    entries = sheet.load(sheet_path) if sheet_path else []\n    matching = match_sources(slips, entries, list(report.rows), int(cfg[\'date_tolerance_days\']))\n    pairs, missed = matching.receipt_pairs, matching.receipt_missing\n'),
   (58, 60, '        paired, _, missed_edits = fx.plans_from_matches(cfg, matching.edit_pairs, matching.edit_missing)\n'),
   (69, 70, '    return WorkPlan(tasks, len(pairs), len(paired), skipped, unmatched, matching)\n'),
   (77, 78, '               f\'기존 영수증 유지 {plan.skipped_receipts}건 · 매칭 확인 필요 {len(plan.unmatched)}건\\n\'\n               f\'대상 무관 경비 {len(plan.matching.excluded_rows)}건 제외 (Concur에서 삭제하지 않음)\')\n'),
   (79, 79, '    for row in plan.matching.excluded_rows:\n        print(f\'  - 대상 무관: 목록 {row.index + 1}행 · 읽힌 날짜 또는 금액이 모든 원본 거래와 다름\')\n'),
   (92, 92, '    driver.matching = plan.matching\n')]),
 ('src/fix_expenses.py',
  'cb713eca9ab45b4ffad2e8481d56507ec5592f0efdefd8fc8c256d170c8668d9',
  'ea445c406f8ee9355c542c930a97f2bf95f3f01893aec7560a586c336f12fa06',
  [(1355, 1355, '    return plans_from_matches(cfg, pairs, missing)\n\n\ndef plans_from_matches(cfg, pairs, missing):\n    """영수증과 통합하여 판정한 매칭을 재사용한다."""\n'),
   (1397, 1399, '    """목록 안정화 후 모든 행을 넘겨 대상별 미확인을 판단한다."""\n    return rows_when_ready(page, tries=tries, wait_ms=wait_ms, allow_incomplete=True)\n'),
   (1405, 1406, '    usable = [r for r in rows if r.expense_id and r.when is not None and r.amount is not None]\n'),
   (1408, 1409, '              "읽지 못해 관련 가능 거래만 보류합니다")\n'),
   (1413, 1414, '    paired, gaps, missing = plans_from_sheet(cfg, rows,\n'),
   (1462, 1463, '        print(f"\\n{skipped}건은 자동 반영하지 않습니다 (대상 무관·매칭 보류·입력 없음)")\n'),
   (1488, 1489, '    return int(bool(missing))\n')]),
 ('src/gui.py',
  'fe40f3da504e06b3c07306dea98f599bf3c562aad7a47f27ce30904f95ea668b',
  '9087382de16b0bcf049eb5ce1f9671d179e741a538b57a158d8a2cb145bdf6d4',
  [(87, 88, '        self.title("Auto-Concur · 대상별 처리판 2026.09.17")\n')]),
 ('src/report_session.py',
  'b18cd586c24b1b233b65803c7bbfde14e08ef093e8c1bcc8a5bd25630ddb6fae',
  '62d2f03c94fb6ae1b06bad1192e9f7d1d5bc40345233cb68eb87ab4c2313df5e',
  [(27, 28, '    return ar.snapshot_signature(rows)\n'),
   (46, 47, '            complete = ar.rows_observed(rows)\n'),
   (66, 67, '    rows = ar.rows_when_ready(page, allow_incomplete=True)\n')]),
 ('tests/test_blank_policy.py',
  '7ef19d81f8362c9dd0b85bda1c299db8c942a7b137cd4a71798cacd7d7e9a5fa',
  '62e0b34df548b69b9f30227e21496774779d5fe2d9c30a56968ce08b03edc70f',
  [(73, 74, '             ar.Slip(tmp_path / \'b.pdf\', date(2026, 8, 1), 2000, \'\', \'B\')]\n'),
   (75, 76, '            ar.Row(1, slips[0].when, 2000, \'\', \'E2\', has_receipt=False)]\n'),
   (77, 78, '    monkeypatch.setattr(ar, \'rows_when_ready\', lambda p, **kwargs: rows)\n')]),
 ('tests/test_matching.py',
  '79d4d2df594680ecffeb866f20ee88553bff4ee509b767ffcd852c94ec69b52f',
  'f211c432654cef485c04cbf0864f64b544adc81ed95f4504c21e736478b5baa6',
  [(20, 21, '    return Row(index, date(2026, month, day), amount, f"{day} | {amount}", expense_id=f"E{index}", vendor=vendor)\n'),
   (44, 46, 'def test_같은_날_같은_금액이_둘이면_보류한다():\n    # 동일 금액 두 건을 순서로 고르지 않는다.\n'),
   (47, 49, '    assert not pairs and "보류" in skipped[0][1]\n'),
   (59, 60, 'def test_가맹점이_구별되지_않으면_보류한다():\n'),
   (61, 63, '    pairs, skipped = match([slip(16, 450000, merchant="라한호텔울산")], rows, 1)\n    assert not pairs and "보류" in skipped[0][1]\n'),
   (66, 67, '    # 전표 둘이 같은 행을 두고 다투면 둘 다 보류한다.\n'),
   (68, 69, '    assert not pairs and len(skipped) == 2\n')]),
 ('tests/test_ready_update.py',
  'c2ccfdb29509831e58044b8dff646ff0385c63c20f272533d65ff64630eb062a',
  'fbfb8bcb6f235c29211ba298f51659293b2d83b8a3cd9b202aa3ee348114dedc',
  [(202, 202, '\n\n\ndef test_target_scope_keeps_unreadable_rows_and_matches_only_safe_ones(page):\n    from src.target_matching import match_sources\n    from pathlib import Path\n    page.set_content(row_html() + row_html(day=\'invalid\', amount=\'9000\', rowid=\'manual\'))\n    rows = ar.rows_when_ready(page, tries=4, wait_ms=1, allow_incomplete=True)\n    assert len(rows) == 2 and rows[1].when is None\n    target = ar.Slip(Path(\'fake.pdf\'), date(2026, 9, 17), 17000, \'\', \'TEST\')\n    batch = match_sources([target], [], rows, 1)\n    assert len(batch.receipt_pairs) == 1 and len(batch.excluded_rows) == 1\n\n\ndef test_target_scope_unknown_amount_only_blocks_related_date(page):\n    from src.target_matching import match_sources\n    from pathlib import Path\n    page.set_content(row_html() + row_html(amount=\'invalid\', rowid=\'manual\'))\n    rows = ar.rows_when_ready(page, tries=4, wait_ms=1, allow_incomplete=True)\n    target = ar.Slip(Path(\'fake.pdf\'), date(2026, 9, 17), 17000, \'\', \'TEST\')\n    assert not match_sources([target], [], rows, 1).receipt_pairs\n\n\n@pytest.mark.parametrize(\'attrs\', [\'aria-busy="true"\', \'aria-rowcount="100"\'])\ndef test_target_scope_still_blocks_loading_or_unseen_rows(page, attrs, monkeypatch):\n    monkeypatch.setattr(ar.concur_ui, \'diagnose\', lambda *a: \'\')\n    page.set_content(\'<div role="grid" \' + attrs + \'>\' + row_html() + \'</div>\')\n    with pytest.raises(ar.AttachError):\n        ar.rows_when_ready(page, tries=4, wait_ms=1, allow_incomplete=True)\n')]),
 ('tests/test_report_session.py',
  '9e70e8b7d1449cc3fc207c295d459dc0c25cc09ac5bbafa12a8ec792655e5c8c',
  '9409817c3d6827c42152beee5195ee3644d85361ede8917f9d1d08d1e166dcb0',
  [(35, 36, '    monkeypatch.setattr(ar, \'rows_when_ready\', lambda page, **kwargs: [])\n')]),
 ('tests/test_rerun.py',
  '6fde876874bb79421ffa2a0998713a0e5bac978a563601f8a09ea1eae9d59e5a',
  'aed6c6b8a22ed4eb6c72ba8a162517e87e3fe56370b0d162e87e7ea75e57679a',
  [(217, 218, '    monkeypatch.setattr(ar, \'rows_when_ready\', lambda page, **kwargs: [\n')]),
 ('docs/behavior.md',
  '43dd5e8224e54e88713f57728752f2f7e4a3ccc77b1256789d92cf731b13fe52',
  'bc72bbec69b52dee7188cc938564ad9fc66c533f2d1754a1e21d7a3e411b9759',
  [(22, 23, '2. `report_session`: Concur 리포트 목록 주소와 전체 관측 목록의 안정성을 연속 확인한다.\n   안정된 행에 값이 없어도 보존하여 대상별 매칭 단계에 넘긴다.\n'),
   (25, 26, '3. `concur_workflow`: 한 번 읽은 목록으로 영수증·입력 작업 계획을 만든다. 영수증과 입력 원본을 통합하여 대상별로 매칭한다.\n'),
   (35, 35, '\n## 대상별 매칭 정책\n\n`target_matching.py`가 GUI C/통합 CLI의 영수증·입력 계획에 같은 매칭을 제공한다.\n개별 CLI도 동일한 보수적 매칭 함수를 사용한다. 목록에서 불완전 행을 먼저 제거하지 않는다.\n\n| 관측 근거 | 처리 |\n|---|---|\n| 읽힌 날짜가 허용 오차 밖이거나 금액이 다름 | 해당 원본 거래와 무관한 후보로 제외 |\n| 모든 원본과 위 조건으로 불일치 | 대상 무관 경비로 표시, Concur 값/영수증은 유지 |\n| 관련 가능한 행에 날짜·금액·ID 누락 또는 ID 중복 | 해당 원본 거래를 보류, 다른 안전한 거래 계속 |\n| 날짜와 금액 모두 미확인 | 어느 원본과도 무관하다고 단정하지 않음; 모든 대상 보류 가능 |\n| 여러 완전 후보 | 기존 가맹점 판별 기준으로 유일하게 구별되면 선택, 아니면 보류 |\n| 여러 원본이 같은 경비 후보를 공유 | 선착순 배정 없이 해당 원본 모두 보류 |\n| 목록 로딩 또는 알려진 전체 행 수 미충족 | 개별 행 문제가 아니므로 목록 처리 차단 |\n\n영수증과 작업지는 승인번호·날짜·금액·가맹점이 같고 각 소스에서 하나씩 있을 때만\n동일 대상으로 합친다. 한 소스 안의 중복 또는 승인번호 없는 거래는 임의 합치지 않는다.\n`--limit`은 매칭 판정 후 적용하므로 앞 N건 제한으로 충돌 검사를 우회하지 못한다.\n수동 생성 여부/오류 아이콘/영수증 존재 여부는 대상 무관의 근거가 아니다.\n\n시작 확인에서는 원문을 포함한 목록을 다시 비교한다. C단계 실행 전·후 조회에서도\n대상 매칭을 재평가하고 연결이 모호해지면 해당 작업을 기록 후 보류한다.\n일부만 처리한 결과는 전체 완료로 표시하지 않고, 원본별 보류 이유와 미해결 건수를 출력한다.\n제외는 프로그램의 작업 대상에서 빼는 뜻이며 Concur 경비 삭제가 아니다.\n'),
   (72, 73, '- 같은 날짜·금액 후보의 순서 배정은 금지한다. 가맹점으로 구별할 수 없으면 관련 거래를 보류한다.\n')]),
 ('launcher_ready.py',
  '378dd1f3063fe265857729899a92dcc05f90790eb74f9c5a2bfa86d0769d4d2c',
  '673b198d411d408f0bfadb6289e42161d84cf26de32a030bcc40e7704e8eeeae',
  [(0, 1, '"""Windows 단일 EXE/폴더 배포의 진입점. --smoke-test는 빌드 검증 전용이다."""\n'),
   (65, 66, '            messagebox.showerror(\'Auto-Concur 실행 오류\', \'프로그램을 시작하지 못했습니다. 빌드한 실행파일과 사용 폴더의 쓰기 권한을 확인해 주세요.\\n\\n\'+detail[-1600:])\n')]),
 ('build_exe.bat',
  '0b9c74a19dbbb722caaf833c621f6fb216b94519cec99fe68b8458f8b411785d',
  '95adf9173b89ce3094189205a44889506c71f3bf1bb9ae03d9c64142cd4c6539',
  [(56, 56, '  --collect-all tksheet ^\n'), (59, 60, '  launcher_ready.py\n')]),
 ('README.md',
  '1e454299910be5756b14ebb113324d4a0ef87aa012d9d51258280d197127c15a',
  '2e50a506cd268f6ebb2c4cc049497952cb7a2884c253e7781715155f58d71201',
  [(4, 4, '\n## 2026-09-17: 대상별 제외와 보류\n\nC단계는 안정된 목록에서 **대상과 무관한 경비는 그대로 두고, 애매한 거래만 보류**합니다.\n- 날짜 또는 금액 중 읽힌 값이 원본 거래와 불일치하면 그 거래의 후보에서 제외합니다.\n- 날짜·금액·ID를 못 읽은 행도 버리지 않습니다. 관련 가능성이 남은 원본 거래만 보류합니다.\n- 날짜와 금액을 모두 못 읽으면 무관함을 입증할 수 없어 여러 거래 또는 전부가 보류될 수 있습니다.\n- 수동 생성, 오류 아이콘, 영수증 유무만으로 제외하지 않습니다.\n- 동일 날짜·금액 후보의 순서 배정을 없앴습니다. 가맹점으로도 구별되지 않거나 여러 원본이 같은 경비를 가리키면 보류합니다.\n- 영수증과 작업지를 함께 매칭합니다. 시작 확인 후와 각 작업의 조회 시점에 대상 연결을 다시 확인합니다.\n- 목록 로딩 중, 알려진 전체 행 수보다 적은 화면, 리포트 변경은 여전히 안전을 위해 차단합니다.\n\n조회 시작·종료일은 첫 화면 달력으로, 작업지 입실·퇴실일은 셀 더블클릭/Enter 또는 `숙박 날짜 · 달력`으로 선택합니다.\n`거래일`은 원본 매칭 정보이므로 편집하지 않습니다.\nWindows 단일 파일은 `setup.bat` 실행 후 `build_exe.bat`으로 `dist/Auto-Concur.exe`를 만듭니다.\n새 코드를 받아도 기존 EXE는 바뀌지 않으므로 다시 빌드해야 합니다. 생성물/실제 Concur 검증 범위는 CI 기록을 확인합니다.\n')])]

for name, before, after, changes in EDITS:
    path = ROOT / name
    source = path.read_text(encoding='utf-8')
    if sha256(source.encode()).hexdigest() != before:
        raise RuntimeError('Source mismatch: ' + name)
    lines = source.splitlines(keepends=True)
    for start, end, text in reversed(changes):
        lines[start:end] = [text]
    result = ''.join(lines)
    if sha256(result.encode()).hexdigest() != after:
        raise RuntimeError('Result mismatch: ' + name)
    path.write_bytes((result.replace('\n','\r\n') if name.endswith('.bat') else result).encode('utf-8'))
    print('Updated: ' + name)
